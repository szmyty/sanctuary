#!/usr/bin/env bash
set -Eeuo pipefail

# if [ -z "${HEALTHCHECK_URL:-}" ]; then
#   echo "HEALTHCHECK_URL is not set. Skipping health check."
#   exit 0
# else
#   echo "HEALTHCHECK_URL is not set. Skipping health check."
#   exit 1
# fi

exit 0
