######################################################################
# @Project      : sanctuary
# @File         : common.env
# @Description  : Common environment variables shared across all containers
#                 in the Sanctuary project. These variables set global settings
#                 such as timezone, locale, user IDs, and group IDs, ensuring
#                 consistency and security across the deployment.
#
# @Author       : Alan Szmyt
# @Date         : 2024-08-23
# @Version      : 1.0
######################################################################

# Timezone setting for all containers. Ensures all logs and operations are synchronized time-wise.
TZ=America/New_York

# Locale settings influence the default format for dates, times, and other locale-specific data.
LANGUAGE=en_US:en
LANG=en_US.UTF-8

# Define a fixed user ID across all containers to ensure consistent file ownership.
# Using a high non-standard UID/GID to minimize conflicts with host system IDs.
SANCTUARY_UID=65532

# Define a fixed group ID across all containers to ensure consistent group ownership of files.
SANCTUARY_GID=65532

# User under which all processes will run inside each container. Helps prevent privilege escalation.
SANCTUARY_USER=sanctuary

# Group under which all processes will run inside each container. Enhances security through consistent access controls.
SANCTUARY_GROUP=sanctuary
